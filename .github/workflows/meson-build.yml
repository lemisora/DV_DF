name: Meson Build

on:
    push:
        branches:
            - actions_github

jobs:
    build:
        name: Build Project
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-python@v1
        - name: Install and build with Meson
          run: |
            pip install meson ninja
            meson setup out
            meson compile -C out
        - name: Run tests
          run: |
            ./out/traductor entrada.txt
            1
            32
            16
            8
            2
            3
            4
            185
            5
            15
            0